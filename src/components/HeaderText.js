import styled from "styled-components";

export default styled.h1`
  font-family: ${(props) => props.theme.font};
  font-size: 36px;
  text-align: center;
  max-width: 80vw;
  vertical-align: top;
  color: white;
`;
